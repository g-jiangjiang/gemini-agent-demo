# Gemini Agent Demo

一个基于 Google Gemini 的轻量级 AI Agent 演示项目。

## 🛠️ 快速开始
1. 安装依赖: `pip install -U google-generativeai python-dotenv`
2. 创建 `.env` 文件并填入你的 `GEMINI_API_KEY`。
3. 运行项目: `python main.py`

## 📂 项目结构
- `main.py`: Agent 核心逻辑与交互入口。
